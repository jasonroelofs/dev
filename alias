alias sup='svn up'
alias sci='svn ci'
alias st='svn st'
alias sad='svn add'

alias cap='USER=jroelofs cap'

alias start-apache="sudo /etc/init.d/apache2 start"

alias pg="ps aux | grep "
alias ng="netstat -an | grep " 

alias gvi='gvim'
alias givm='gvim'
alias gvim='gvim -X'

alias ls="ls --color"

# SIGH @ UBUNTU
alias sudo="sudo env PATH=$PATH"

# Again, ubuntu packaging strikes again. "ack" is a japanese kanji converter tool >.<
alias ack="ack-grep"
